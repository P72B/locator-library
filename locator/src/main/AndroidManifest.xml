<manifest package="de.p72b.locator"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="de.p72b.locator.lib_locator_util.ContextProvider"
            android:authorities="${applicationId}.lib_locator_util.contextprovider"
            android:exported="false" />
    </application>
</manifest>
