<manifest package="de.p72b.locator"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name=".util.ContextProvider"
            android:authorities="${applicationId}.util.contextprovider"
            android:exported="false" />
    </application>
</manifest>
